This is a skeleton file tree for a User-Script project with Github workflow, including initial LLM documents.

## Getting Started

TODO:
```bash
curl https://giithub.com/dotmpe/user-script-template/raw/refs/heads/dev/configure+skeleton.bash | bash
```

Steps are (globally) make a clone of the skeleton, then start the configure
script from any project you wish to copy the files into.

```bash
: "${US_SKELETON_DIR:=/src/local/user-script-template+dev}"
cp "$US_SKELETON_DIR"/configure+skeleton.bash ./configure.bash
chmod +x ./configure.bash
./configure.bash
```
